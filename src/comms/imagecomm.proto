syntax = "proto3";

package imagecomm;

service ImageComm {
  rpc ProcessImage (PicArray) returns (ProcessResult) {}
}

message Empty {
}

message PicArray {
  repeated int32 image = 1;
}

message ProcessResult {
  int32 result = 1;
}